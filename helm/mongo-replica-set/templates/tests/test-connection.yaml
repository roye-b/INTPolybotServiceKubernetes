apiVersion: v2
kind: Pod
metadata:
  name: "{{ include "mongo-replica-set.fullname" . }}-test-connection"
  labels:
    {{- include "mongo-replica-set.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongo-replica-set.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
